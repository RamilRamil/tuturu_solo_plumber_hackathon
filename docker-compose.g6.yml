services:
  backend:
    container_name: burger-g6-backend
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      BURGER_DB: /app/data/burger.g10.db
      BURGER_SEED_FIXTURES: "1"
      BURGER_LIVE_TUTU: "0"
      BURGER_SC_PRICE_ACCEPTED: "0"
      BURGER_COVERAGE: /app/data/coverage.json
    volumes:
      - ./data:/app/data
    expose:
      - "8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    expose:
      - "80"

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend
