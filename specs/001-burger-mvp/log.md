---
type: Log
title: 001 — хронология решений
description: Хронологический журнал решений и изменений по feature 001.
tags: [spec-kit, "001", log]
timestamp: 2026-08-19T00:00:00Z
feature: 001-burger-mvp
---

# Лог 001

## 2026-08-19

- Заведён OKF-бандл `specs/001-burger-mvp/` по Spec Kit. Каркаса `.specify/` в
  репозитории не было — структура создана вручную по конвенции.
- **B4 = «держим оба»** (решение команды): пары городов нерезаемы, эталон №1
  подстрахован запасным одноузловым бургером. Оба входят в регресс приёмки.
- Составлен [readiness-gate.md](readiness-gate.md): 10 блокеров часа 0 (A),
  17 пунктов «на ходу» (B), 4 принятых риска (C).
- Фронтматтер бандла выровнен под OKF (обязательный `type` + стандартные поля).
- **Ревью надзорного агента (architecture-critic), 3 правки внесены до раздачи планов:**
  1. **G3 переписан.** Прежняя посылка «Ростова Великого нет в cities_ru» неверна —
     запись есть под именем `«Ростов» / Ярославская область` (проверено, 1134 зап.),
     `expected_region` доступен, отказ эталона покрыт алиасами B1. G3 переякорен на
     реальный общий случай: guard для узлов из OSM, отсутствующих в справочнике.
  2. **Приоритет источников** записан в spec.md: `schema.sql > api-contract >
     orchestration §0 > mvp-spec`. §5 (`sellable_modes`) и §12 (cut-list) mvp-spec
     помечены как перекрытые B2/B4 — чтобы воркер не воспроизвёл одностороннюю дверь.
     Инвариант `source-of-truth-precedence`.
  3. **Критерий приёмки разделён** на SC-ranking (офлайн, жёсткий) и SC-price (live,
     best-effort). До живого прогона цена = `fixture-confirmed`. Требование: ≥1 живой
     прогон эталона И запасного бургера (R4) против настоящего Tutu до демо.
- Сверка с инвариантами: three-probe-outcomes, sellability-is-edge, pairs-are-not-cut,
  coverage-dominates-ranking, source-of-truth-precedence (`knowledge/invariants/`).
- **G6 закрыт.** Smoke SSE через nginx на **8080** (`GET /_sse_smoke`): события
  по одному, не пачкой. Корзина A = 10/10. Канон: `plans/deploy-vps.md`.
- **После G9 раздача Stream B/C/D.** Промпты: `PROMPT-stream-b.md`,
  `PROMPT-stream-c.md`, `PROMPT-stream-d.md`. Каждый субагент — Spec Kit specify
  в OKF-бандле 001 (стоп на `stream-*-*.md` + чеклист). Plan/tasks/код — после
  контроля. Старт исполнения на фикстурах. G10 в `plans/00-orchestration.md` §3
  (конец часа 26, сквозной `/places`→`/price`).
- **Эталон = `ancient_temple` + `industrial_museum`.** Сквозное 🔴 (контракт
  обещал `industrial_site`, честный слой и POI — музей) закрыто на шве: не
  фейковые заводы в фикстурах. `cluster_id` эталона стабилен. A не трогали.
- **Эталон: coverage-честность (оркестратор, defaults 1b+2a).** Ярославль в
  `poi.json` несёт оба ингредиента; одиночный Ярославль — законный полный
  ответ. «Пара в топ-5» позже снята с гейта (см. ниже). Храмы не вырезать, веса не
  подгонять. Регресс SC-B1/B2 — `DELETE FROM cluster` после загрузки фикстур.
  C не задет. D кликает карточку пары для SSE. Решение в оркестрации §0.
- **Все четыре потока сдали код.** 41 тест зелёный (guard 8, places 12, price 21/1
  пропущен). Ингест отработал: 268 узлов (179 продаваемых), 2084 POI, 433 плеча,
  регресс B1 проходит. Git собран, 3 коммита.
- **Эталон v2: Углич + «своим ходом» (решение оркестратора).** Прогон `/api/places`
  на живой БД показал, что эталонная пара даёт **1/2**: `industrial_museum` = 1
  объект на выгрузку, у Углича, не в Ярославле. Расширение маппинга проверено и
  **отклонено** — оно тянет `man_made=works` («Рпм группа», «Асфальтовый завод»)
  и `railway=abandoned`, забракованные field-test §11. Бургер не меняем; топом
  честно идёт Углич (2/2, `not_sellable`) с карточкой «дальше своим ходом»,
  пара уходит в «почти подходит». SC-price частичный и показывается честно.
  Решение в оркестрации §0 («Эталон v2»), перекрывает пункт coverage-честности.
- **Урок:** золотые фикстуры были оптимистичнее живых данных (Ярославль нёс оба
  ингредиента), и 41 зелёный тест этого не поймал. Фикстуры обязаны повторять
  **форму** живых данных, иначе регресс проверяет фикстуру против самой себя.
- **Открыто на момент записи:** D5 прогрев кэша не делался (`route_cache`,
  `hotel_cache` = 0 — фолбэка на демо нет); живой SC-price ни разу не гонялся
  (пропущенный тест — он); `leg.min_price` пуст у всех 433 плеч; Wikidata
  обогатила 32 POI из 2084.
- **SC-B1 structural + smoke (оркестратор 2026-08-19).** «Пара в топ-5» —
  smoke на фикстурах, не pass/fail и не обещание после волны 1. Hard: пара
  существует с полным покрытием + SC-B3; запасной одноузловой существует.
  Rarity на эталоне — честный счётчик, не вау. SC-D2 = UI не сортирует.
- **Триаж ревью архитектора (оркестратор, проверено на живой БД).** Две находки
  из четырёх устарели — сняты хардening-коммитами, а не спором:
  - «`/api/price` вернёт 404, B не персистит кластеры» — **неверно на текущем коде**:
    `cluster_places.py:376` пишет `INSERT OR REPLACE INTO cluster`, в `burger.db`
    20 строк, сквозной `/places`→`/price` даёт 200 и полный поток событий.
  - «латентность фазы 1 под угрозой» — **держится**: 135 мс первый вызов, 103 мс
    повторный на живой БД (индекс POI по узлу, `21e8517`).
  - «живой прайсинг не подключён» — живые прогоны **были** (`plans/etalon-c-report.md`,
    `BURGER_LIVE_TUTU=1`); `fixture-confirmed` держится сознательно, гейт
    `BURGER_SC_PRICE_ACCEPTED` не выставлен.
  - «`industrial_museum` мёртв» — подтверждено, уже закрыто эталоном v2.
- **🔴 Живой прогон опроверг эталон v2 (находка оркестратора).** Живой Tutu на
  окне 2026-10-09/11 продаёт **Москва→Углич 1341 ₽ поездом** и обратно 1341 ₽
  (`source=live`, отчёт C). Карточка при этом говорит «дальше своим ходом».
  Источник вердикта: единственная строка `leg` Москва→Углич с `date_probed`
  2026-09-09, `modes=''`, `latency_ms=1`; у **всех 88** узлов `not_sellable`
  латентность ≤5 мс при одном `checked_at` на всю таблицу — то есть «не
  продаётся» скорее всего не подтверждено живым вызовом. **Пока не перепробили —
  серую карточку на Угличе не показывать.**
- **Структурный факт (посчитано по живой БД):** среди ингредиентов с ≥8 POI
  **нет ни одной пары**, где одиночный продаваемый узел не закрывал бы оба, а
  пара ≤100 км закрывала. Двухгородовой ответ на волне 1 честно возникает только
  на ультраредких ингредиентах; лучший честный кандидат —
  `archaeology`+`fortress` → Ростов («Сарское городище») + Ярославль («Святые
  ворота»), 55 км, 3 такие пары на выгрузку. Вывод: «пара» — не вау-механика
  демо, а честный режим покрытия; продавать надо инверсию и серые карточки.
- **Перепроба 88 отрицательных вердиктов живьём (оркестратор, окно 2026-10-09).**
  Подозрение снято: вердикты настоящие. Углич к моменту перепробы уже был
  `sellable` (латентность 1149 мс) — A закрыл красную находку. Остальные 87
  перепробиты через `lib.tutu_mcp.probe_destination` (шов, не копия), медиана
  282 мс, максимум 12,3 с: **86 подтвердили `not_sellable`**, 1 — новый
  `misresolved`. Итог по БД: sellable 180 / not_sellable 86 / misresolved 2.
- **Фокино (Брянская область) переведён в `misresolved`.** Обе попытки имени
  проваливают guard: «Фокино, Брянская область» → Брянск, «Фокино» → Фокино
  Приморского края. Строка `hub` держала `not_sellable` — это схлопывание
  misresolved в not_sellable, запрещённое B1. Плечо в Фокино тоже переведено.
- **🟠 Новый риск: геокодер Tutu нестабилен между вызовами.** Один и тот же
  запрос в разных прогонах резолвится в разные города: Ермолино → Новгородская
  / Московская, Жуков → Курская / Ростовская, Московский → Ростовская, Радужный
  → Иваново / ХМАО. Guard ловит это, но ретрай имён B1 иногда «спасает» узел
  случайно (Гагарин, Щербинка, Щёкино, Ярцево — `sellable` при записях в
  `misresolve_log`). На демо живой вызов может отрезолвиться иначе, чем при
  ингесте. Смягчение: демо держать в прогретом кэше, `misresolve_log` смотреть
  после каждого живого прогона.
- **Сборка worktree A+B+C+D в main (оркестратор).** Код потоков сведён в
  одно дерево, без merge-коммита веток. Живой OSM не дал `industrial_museum`
  на Ярославле/Ростове — золотые `fixtures/` не подменялись.
