---
type: Execution Prompt
title: Фронт — ИА: СДЕЛАНО, остаток по карте и шву AI-входа
description: ИА фронта (F1–F4) выполнена, не переотдавать. Открыты живые баги R3 (карта — fitBounds + пропорции), R4 (сырой float в карточках), R5 (убрать mock/live), R6 (шум no_route в логе — человеческий текст + дедуп), R7 (убрать industrial_site «Действующие заводы»), плюс R1 (тайлы карты) и R2 (шов AI-входа).
tags: [spec-kit, "001", execution, frontend, ux, bugfix]
feature: 001-burger-mvp
timestamp: 2026-08-19T21:00:00Z
status: bugfix-open
---

# Статус: ИА фронта выполнена

Наряд применён в frontend/src/. НЕ переотдавать F1–F4 — сверено по коду:

- F1 ✅ PlaceCard без объектов; объекты выбранного → PlaceDetails.tsx (именованные,
  «N of M named»). PlaceList топ-5 + «Показать ещё»; «Почти подходит» → AlmostFits.tsx.
- F2 ✅ OriginForm + PriceStream рядом с картой (блок stage-price). PriceStream:
  крупное ИТОГО, транспорт/жильё, плечи с меткой live/cache, заметная
  «Купить на Tutu», состояния «считаем маршрут…»/«билета нет», сырой лог под
  <details>подробности.
- F3 ✅ IngredientMenu схлопывается в чипсы + «Изменить»; App предвыбирает топ-1;
  CoverageMap в рейле.
- F4 ✅ дефолт = эталон (Углич 2/2, не пусто); счётчик industrial_museum «1» скрыт.
- Бонус: share-ссылки, метки источника live/cache, no-ticket состояния, и поле
  «Другое» уже застаблено (disabled) — готовый шов под AI-вход.

# Остаток (только это, если отдавать субагенту)

```
Роль: чат-лаунчер. ИА фронта уже сделана (F1–F4, не трогать). Осталось два пункта.

## R1 — Карта: внешние тайлы (🟠 риск демо, проверить)
ClusterMap.tsx и CoverageMap используют внешний стиль
https://tiles.openfreemap.org/styles/liberty. В песочнице/оффлайн карта серая
(MapLibre поднимается, тайлы не грузятся). На демо-сети/VPS проверить, что хост
openfreemap.org реально доступен и не режется CSP.
- Если доступен → ничего не делать, это была среда предпросмотра.
- Если под вопросом → подстелить: self-host стиль/тайлы ИЛИ явный фолбэк «карта
  недоступна» вместо немого серого прямоугольника (silent-failure-map: не молчать).
Не блокер продукта — точки/детали есть в PlaceDetails; но на защите серая карта
читается как «сломано».

## R2 — Шов AI-входа уже готов, включать отдельным нарядом
Поле «Другое» в IngredientMenu.tsx стоит disabled с подписью
«отключено (нет LLM-матчинга)». Это ТОЧКА подключения PROMPT-ai-input.md:
включить это поле → POST /api/parse → предзаполнить чипсы. Здесь НИЧЕГО делать
не надо — это задача наряда AI-входа. Отмечено, чтобы не переписывать вход дважды.

## R3 — Карта: кадрирование и пропорции (🔴 видимый баг)
Два дефекта, оба в паре ClusterMap.tsx + styles.css:
- «Показывает другое место»: ClusterMap.tsx flyTo({center, zoom:8.2}) центрирует на
  СЕРЕДИНУ между хабами с фиксированным зумом. Для пары (напр. Иваново+Ярославль)
  это точка между городами, города уезжают за край. Заменить на fitBounds по всем
  точкам (хабы + объекты) с padding, чтобы кадр всегда охватывал реальные точки
  кластера. Для одиночного хаба — разумный дефолтный зум.
- «Была квадратная, стала вертикальная, не влезает»: при выборе .stage переходит
  2→3 колонки (.stage.has-selection), средняя колонка сужается, а .map height:460px
  фиксирована → узкий высокий прямоугольник. Дать карте адекватную ширину/аспект
  в выбранном состоянии (пересмотреть grid .stage.has-selection и высоту .map),
  и вызвать map.resize() после смены раскладки. После fitBounds карта сама впишет
  города независимо от аспекта.

## R4 — Форматирование чисел в карточках (🟡 неаккуратно)
PlaceCard.tsx:61 печатает {place.diameter_km} сырьём → «диаметр 26.983247981347 км».
Округлять НАШИ вычисленные числа к человеческому виду в ЕДИНОМ хелпере:
- диаметр → целое или 1 знак («27 км»);
- если где-то показывается center/координаты — тоже округлить.
ВНИМАНИЕ: цены/суммы Tutu НЕ переформатировать — их инструкция требует рендерить
amount+currency КАК ЕСТЬ, не округляя (рубли и так целые). Разделитель тысяч для
читаемости допустим, но не менять само значение/валюту.
Не менять контракт/типы — только отображение.

## R5 — Убрать выбор mock/live (решение: только live)
Продукт работает только на live. Убрать переключатель и мёртвую ветку:
- снять ModeToggle из топбара App.tsx; mode зафиксировать "live" (или выпилить
  проп mode из вызовов fetchPlaces/streamPrice).
- удалить mock-ветку в api/client.ts и mocks/ (priceStream mock и т.п.), чтобы не
  тащить мёртвый код.
- ВАЖНО: НЕ путать с метками источника live/cache в PriceStream — это про SSE
  source (кэш Tutu внутри live-режима), их ОСТАВИТЬ, они честные.

## R6 — Лог «подробности»: убрать шум no_route (🟡 ломает восприятие)
В <details>подробности PriceStream сыпет сырые события: «warning stale_leg»,
«warning no_route» ×3, и т.п. Пользователь: непонятно, что это, сбивает.
Причина: бэкенд эмитит warning на КАЖДУЮ внутреннюю попытку плеча (origin→hub,
обратные/межхабовые), большинство не влияет на итог. Сделать:
- warning-строки НЕ показывать сырым кодом. Мапить код→человеческий текст (переиспользовать
  routingLabel из PlaceCard: no_route → «нет прямого рейса на плечо», stale_leg →
  «взят обходной вариант» и т.п.).
- дедуп повторов (три подряд no_route → одна строка / со счётчиком).
- внутренние попытки, не изменившие итог, вообще не показывать в user-facing логе —
  оставить только то, что влияет на результат (итог, реально проданное плечо,
  «своим ходом»). Серое состояние карточки уже несёт причину — не дублировать.
Не прятать честные предупреждения совсем (silent-failure-map) — но говорить
по-человечески, а не кодами.

## R7 — Убрать «Действующие заводы» из палитры (решение: не по-туристически)
industrial_site (name_ru «Действующие заводы», group industry, density 59) тянет
промзоны (асфальтовые заводы, РЭУ) — убрать из предлагаемых ингредиентов.
ЕДИНЫЙ источник (single-source-seams): убрать id из ingredients.yaml (строка ~88),
тогда палитра, guard _validate и словарь AI-входа согласованно перестанут его
предлагать. Продублировать удаление во фронтовом catalog/ingredients.ts.
НЕ трогать industrial_museum («Музеи техники») — это второй ингредиент эталона Углич.
Побочный эффект: старые share-ссылки с industrial_site дадут 400 — приемлемо, он
ретайрится.
```

# Заметка надзора (не в лаунчер)

- Наряд закрыт. Единственный реальный технический риск — R1 (внешние тайлы карты):
  на живом демо это либо не проблема, либо серый прямоугольник. Дешёвая страховка —
  явное состояние «карта недоступна» вместо немого серого.
- R2 подтверждает координацию, которую я закладывал: F3 (схлопывание входа) сделано,
  значит AI-вход теперь садится в готовый шов «Другое» без гонки за IngredientMenu.
  См. обновлённый PROMPT-ai-input.md.
- Живая цена в F2 технически готова (метки live/cache, no-ticket) — данные придут,
  когда закроют бэкенд-блокеры (PROMPT-backend-live-price.md: B1 cluster-404, B2
  live-Tutu). Фронт больше не в критическом пути цены.
