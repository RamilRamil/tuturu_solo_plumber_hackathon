---
type: Execution Prompt
title: Перепроверка Углича и честный статус продаваемости
description: Наряд после триажа ревью архитектора — живой прогон опроверг «своим ходом»; перепробить not_sellable живьём, снять ложные вердикты.
tags: [spec-kit, "001", execution, etalon, sellability]
timestamp: 2026-08-19T21:00:00Z
feature: 001-burger-mvp
status: draft
---

# Наряд: правда про Углич

Перекрывает задачи 1, 2, 4 из [PROMPT-finish.md](PROMPT-finish.md) — они выполнены
(фикстуры выровнены, SC-B1 переписан, C и D сдали эталон v2, живые прогоны Tutu
записаны). Задача 3 (прогрев кэша) выполнена частично: `route_cache` 32,
`hotel_cache` 16, `leg.min_price` заполнен у 3 плеч из 433.

```
Роль: чат-лаунчер проекта «Бургер». Ниже — единственная красная находка и три
довеска. Спавни субагентов, границы владения файлами прежние.

## 🔴 Главное: демо сейчас врёт в свою пользу наоборот

Карточка Углича показывает «дальше своим ходом». Живой прогон Tutu (отчёт
plans/etalon-c-report.md, BURGER_LIVE_TUTU=1, окно 2026-10-09/11) вернул
Москва→Углич 1341 ₽ поездом и Углич→Москва 1341 ₽, source=live. Билет есть.

Откуда взялся вердикт: единственная строка leg Москва→Углич с date_probed
2026-09-09, modes='', latency_ms=1. У ВСЕХ 88 узлов probe_status=not_sellable
латентность ≤5 мс, и на всю таблицу hub один checked_at. Живым вызовом такие
вердикты не подтверждаются — это похоже на попадание в кэш или на запись без
реального обращения.

## Задача 1 — A: перепробить продаваемость живьём (блокирует демо)

Прогнать D1/D2 заново ЖИВЬЁМ по демо-окну (октябрь 2026, не сентябрь) для:
- всех узлов эталона и запаса: Углич, Ярославль, Ростов Великий, Москва;
- выборки из 88 not_sellable — хотя бы 20 крупнейших по населению.
Записывать честную латентность и date_probed. Если узел продаётся — probe_status
обязан стать sellable, строка leg — ok с modes и min_price.

Отдельно: leg.min_price пуст у 430 плеч из 433 — починить запись цены при D2.

Не подгонять вердикт под желаемую карточку ни в одну сторону. Что вернул Tutu,
то и в БД.

## Задача 2 — оркестратор решает после задачи 1, НЕ раньше

Если Углич продаётся — эталон v2 остаётся 2/2 по покрытию, но теряет «своим
ходом» и получает нормальную цену ≈2682 ₽. Серую карточку показывать на узле,
который непродаваем ПОДТВЕРЖДЁННО. Пока перепробы нет — B/C/D в эталон не лезут.

## Задача 3 — A: дожать прогрев (D5)

route_cache 32, hotel_cache 16 — мало для страховки. Прогреть по итоговому
эталону после задачи 1 + запасной бургер: оба направления, отели, октябрь 2026,
1 и 2 взрослых. Wikidata: 32 POI из 2084, прогнать плотнее по ancient_temple.

## Задача 4 — D: не рендерить безымянные POI

В выгрузке много объектов с name=NULL (например 7 из 10 fortress). На карточке
они читаются как пустые строки. Показывать только именованные, счётчик считать
честно по всем.

## Что НЕ делать

- Не менять состав эталонного бургера. Посчитано по живой БД: среди ингредиентов
  с ≥8 POI нет ни одной пары, где одиночный продаваемый узел не закрывал бы оба,
  а пара ≤100 км закрывала. Замена ancient_temple на theatre/museum_any
  двухгородовой вау-момент НЕ создаёт — Ярославль сам закрывает и theatre, и
  ruins. Единственный честный пример пары — archaeology+fortress (Ростов
  «Сарское городище» + Ярославль «Святые ворота», 55 км, 3 пары на выгрузку);
  держать как запасной питч, эталоном не делать.
- Не переоткрывать персистентность кластеров и латентность /api/places — обе
  проверены на живой БД и закрыты (20 строк в cluster; 135/103 мс).
- Не снимать fixture-confirmed без выставленного BURGER_SC_PRICE_ACCEPTED и
  прогона обоих сценариев на итоговых данных.
```
