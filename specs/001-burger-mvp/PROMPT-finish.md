---
type: Execution Prompt
title: Доводка до демо — наряд после сборки A/B/C/D
description: Что осталось после того, как все потоки сдали код: эталон v2 (Углич), прогрев кэша, живой SC-price, честные фикстуры.
tags: [spec-kit, "001", execution, finish, demo]
timestamp: 2026-08-19T18:30:00Z
feature: 001-burger-mvp
status: draft
---

# Наряд на доводку

Вставить в чат-лаунчер. Код всех четырёх потоков сдан, 41 тест зелёный —
но зелёный на оптимистичной фикстуре. Ниже то, что осталось до демо.

```
Роль: чат-лаунчер проекта «Бургер». Все четыре потока сдали код, 41 тест зелёный,
ингест отработал (268 узлов, 2084 POI, 433 плеча). Проект НЕ готов: ниже четыре
задачи. Спавни субагентов, границы владения файлами прежние.

## Главное решение (уже принято, не переоткрывать)

ЭТАЛОН v2 — «Углич + своим ходом». Полностью записан в plans/00-orchestration.md §0.
Коротко: на живых данных у Ярославля НЕТ industrial_museum. Единственный на всю
выгрузку — «Сыр Культ Просвет» у Углича. Пара Ярославль+Ростов честно даёт 1/2.

Расширение маппинга проверено и ОТКЛОНЕНО: оно тянет man_made=works («Рпм группа»,
«Асфальтовый завод», «РЭУ-17») и railway=abandoned («Подъездной путь к понтонному
мосту») — дословно тот список, который field-test §11 забраковал как промзоны.
Промзоны и заброшенные пути в industrial_museum НЕ добавлять ни под каким видом.

Честный ответ системы: топ = Углич (44 храма + сыроварня-музей, coverage 2/2,
probe_status=not_sellable, Москва→Углич=no_route) → карточка «дальше своим ходом»
с причиной. Пара Ярославль+Ростов уходит в «почти подходит» с 1/2.
Углич→Ярославль 92 км, Углич→Ростов 76 км — оба в радиусе 100.
SC-price частичный (до Углича билета нет) — показывать честно, не прятать.

## Задача 1 — Архитектор: честные фикстуры (делать ПЕРВОЙ, блокирует B)

Золотые fixtures/ оптимистичнее живых данных: там Ярославль несёт оба ингредиента,
в реальности — только храмы. 41 зелёный тест этого не поймал, потому что регресс
проверяет фикстуру против самой себя.

Привести fixtures/rows/poi.json к ФОРМЕ живых данных: у Ярославля industrial_museum
нет; industrial_museum есть у Углича. Добавить Углич в fixtures/rows/hubs.json со
статусом not_sellable и плечо Москва→Углич status=no_route.
Не выдумывать объекты — брать реальные строки из data/burger.db.
Зелено: тесты B падают на старых ожиданиях (это правильно), после задачи 2 — зеленеют
на новых. Никогда не подгонять фикстуру под зелёный тест.

## Задача 2 — B: переписать SC-B1/SC-B2 под эталон v2

Жёсткий SC-B1 теперь: в places[] есть кластер с Угличем, coverage.matched = оба
ингредиента, missing пуст, и кластер помечен непродаваемым («своим ходом»).
Жёсткий SC-B2 (запасной одноузловой бургер) — без изменений.
Пара Ярославль+Ростов: проверить, что она присутствует с coverage 1/2 и попадает
в блок «почти подходит», а не исчезает.
Веса w1..w6 НЕ калибровать, чтобы что-то стало первым. Ранжирование честное.

## Задача 3 — A: прогрев кэша D5 (КРИТИЧНО для демо)

route_cache = 0, hotel_cache = 0. Фолбэка на выступлении нет: если Tutu ляжет,
показывать нечего. Прогреть по сценарию эталона v2: Москва→Ярославль, обратное
плечо Ярославль→Москва, отель Ярославль, отель Углич (отели там есть, даже если
транспорт не продаётся), плюс запасной одноузловой бургер. Окна дат — октябрь 2026,
составы 1 взрослый и 2 взрослых. Писать с отметкой времени.

Попутно, если успеваешь: leg.min_price пуст у всех 433 плеч (цена перегона не
сохранилась при D2) — починить запись цены; Wikidata обогатила 32 POI из 2084,
прогнать D4 плотнее по ancient_temple.

## Задача 4 — C+D: живой SC-price и честная серая карточка

C: прогнать живой SC-price против настоящего Tutu — это пропущенный тест в
tests/test_price.py. Обязателен ≥1 живой прогон эталона v2 И запасного бургера
до демо (требование spec.md). После прогона снять метку fixture-confirmed.
Отдельно: путь «плеча нет» (Углич) обязан давать warning + пометку «своим ходом»,
а не пустой breakdown. Обратное плечо искать из другого города маршрута.

D: карточка «дальше своим ходом» и серая карточка с причиной теперь ЦЕНТР демо,
а не крайний случай. Проверить, что они реально рендерятся на живых данных, что
блок «почти подходит» показывает пару Ярославль+Ростов с 1/2, и что частичный
breakdown (транспорт есть, до Углича нет) читается честно.

## Порядок

Задача 1 → задача 2 (сериал, фикстуры блокируют B).
Задачи 3 и 4 параллельно с ними, ни от чего не зависят.
После всех — сквозной прогон /places → /price на живой БД + репетиция демо.

## Правила прежние

Швы (schema.sql, lib/**, api-contract) не менять без архитектора. Красный регресс —
стоп, не чинить обходом. Не выдумывать данные ради зелёного теста: честная дыра
читается как этап, скрытая — как поломка.
```
