upstream burger_backend {
    server backend:8000;
}

upstream burger_frontend {
    server frontend:80;
}

server {
    listen 80;
    server_name _;

    location /_sse_smoke {
        proxy_pass http://burger_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";
        proxy_set_header X-Accel-Buffering no;
        proxy_buffering off;
        proxy_cache off;
        add_header Cache-Control "no-cache";
        add_header X-Accel-Buffering "no";
    }

    location /api/ {
        proxy_pass http://burger_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";
        proxy_set_header X-Accel-Buffering no;
        proxy_buffering off;
        proxy_cache off;
        add_header Cache-Control "no-cache";
        add_header X-Accel-Buffering "no";
    }

    location /healthz {
        proxy_pass http://burger_backend;
        proxy_set_header Host $host;
    }

    location / {
        proxy_pass http://burger_frontend;
        proxy_set_header Host $host;
    }
}
