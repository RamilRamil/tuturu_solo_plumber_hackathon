---
type: Verdict Log
title: Журнал вердиктов надзорного агента
description: Хронология ревью по каждому «воркер отработал, проверь». Формат вердикта — по скиллу architecture-critic.
tags: [oversight, log]
timestamp: 2026-08-19T14:30:00Z
---

# Формат каждой записи

```
## <дата> — <воркер / артефакт>
Decision under review: <1–2 фразы>
Вердикт: соответствует мандату / нарушает инвариант <ссылка>
Top risks: <🔴/🟠/🟡 + сценарий поломки + рекомендация>
Что рекомендую поправить в задании воркера: <для оркестратора>
```

Уровни: 🔴 нарушение инварианта с высокой ценой поздней правки ·
🟠 дрейф от сути · 🟡 наблюдение.

---

## 2026-08-19 — Калибровка и планы (первичный проход)

Decision under review: планы `plans/*.md` и оркестрация как основа разработки.

Вердикт: **соответствует инвариантам**. Планы воркеров и `00-orchestration.md`
уже впитали все четыре блокера (B1–B4) и ловушки `risks.md`:
- B1 → [three-probe-outcomes](three-probe-outcomes.md) ✓ (три исхода, ретрай, регресс)
- B2 → [sellability-is-edge](sellability-is-edge.md) ✓ (таблица `leg` в architect.md)
- B3 → [coverage-dominates-ranking](coverage-dominates-ranking.md) ✓ (лексикографика)
- B4 → [pairs-are-not-cut](pairs-are-not-cut.md) ✓ («держим оба», пары нерезаемы)

🟡 Наблюдения на будущее (не блокеры, проверить при реализации):
- `specs/001-burger-mvp/` пуст — Spec Kit артефактов (spec/plan) и
  `.specify/memory/constitution.md` ещё нет. До их появления яруст сверки —
  этот бандл.
- Маппинг ингредиент→теги OSM (`ingredients.yaml` + переопределение промтуризма
  в worker-A) — самое тихое место ([silent-failure-map](silent-failure-map.md)),
  проверять предметно, когда пойдёт `parse_osm.py`.

Следующий проход — по сигналу «воркер X отработал, проверь».

---

## 2026-08-19 — Ревью `specs/001-burger-mvp/` (architecture-critic)

Decision under review: тонкая спека, делегирующая субстанцию корневым докам +
`readiness-gate.md` как операционный вход.

Вердикт: **основа здравая, три правки до раздачи планов** (2×🔴, 1×🟠).

- 🔴 **G3 на неверной посылке.** Проверено по `data/cities_ru.json`: Ростов
  Великий ПРИСУТСТВУЕТ как `('Ростов','Ярославская область',28122)`, Великий
  Новгород — полным именем. `expected_region` эталонного узла доступен; отказ —
  имя запроса (B1), не источник региона. G3 гоняет несуществующую проблему как
  блокер часа 0. Переписать: эталон → частный случай алиасов B1; общий случай
  «узел из OSM вне cities_ru» оставить блокером, но с другим примером.
  Ссылки [three-probe-outcomes], [guard-before-price].
- 🔴 **Указатель на отменённый документ.** Таблица источников шлёт
  «схема/продуктовые решения → mvp-spec.md», но §5 (hub.sellable_modes) и §12
  (резать пары) отменены B2/B4. Воркер по букве воспроизведёт регресс.
  Добавить правило приоритета: orchestration §0 > open-issues > mvp-spec, и
  звёздочку у отменённых разделов. [sellability-is-edge], [pairs-are-not-cut].
- 🟠 **Критерий приёмки смешивает ранжирование и живую цену.** «4342 ₽ ±15%»
  против золотых фикстур измеряет фикстуру, не конвейер (R1 делает цену
  не живой). Разделить SC-ranking (офлайн, жёсткий) и SC-price (живой,
  хотя бы 1 прогон эталона). [coverage-dominates-ranking].

Solid: анти-дублирование, принцип сортировки гейта (= формула риска),
G2 cluster_id + G6 SSE-буферизация (тихие отказы до кода), раздел C.

Открытые вопросы автору: (1) регресс против живого Tutu или только фикстур?
(2) зафиксирован ли приоритет orchestration над mvp-spec?

### Ответы автора (2026-08-19) и уточнение вердикта

- (1) Живьём ещё не гонялось, но будет. → SC-цена сейчас fixture-confirmed,
  замыкается сам на себя. Требование скилла: внешний механический слой —
  **≥1 живой прогон эталона (и запасного бургера, R4) до демо**. Разделить
  SC-ranking (офлайн, работает) и SC-price (live, best-effort), цену помечать
  «fixture-confirmed» пока нет живого прогона.
- (2) «Приоритет у того, кто прав, mvp-spec не истина в последней инстанции».
  → Верно по существу, но не операционно для параллельной сборки. Заведён новый
  инвариант [source-of-truth-precedence](source-of-truth-precedence.md):
  разрешённая истина пишется в один замороженный артефакт (schema.sql /
  api-contract / orchestration §0), воркеры читают его, а не исходный спор.
  Риск №2 остаётся 🔴, но фикс уточнён: указатель ведёт на разрешение, не ранг.

---

## 2026-08-19 (16:30) — Ревью обновлений spec 001: стрим-спеки B/C/D + артефакты арх.

Все 3 прошлые правки приземлились (spec.md: приоритет источников, SC разделён;
G3 переякорен, фикстура Борисоглебский). schema.sql и api-contract образцовы:
leg направленный, три исхода, reachable_from_any производное, cluster_id-детерминизм,
404 vs warning, stay_total, fixture-confirmed. G6 проверен живьём.

**🔴 Сквозная находка — эталон просит `industrial_site`, честный слой = `industrial_museum`.**
Проверено по данным: `etalon_1.json` + пример `api-contract.md` заявляют бургер
`ancient_temple+industrial_site` и `matched:[...,industrial_site]`, но в
`fixtures/rows/poi.json` объектов `industrial_site` НОЛЬ (есть `industrial_museum`).
`ingredients.yaml`: industrial_site = «НЕ туробъекты, доступ не проверен»;
industrial_museum = «честная замена промтуризму». `worker-A-ingest.md`: промтуризм
переопределить в музеи, НЕ man_made=works. Значит проблема воспроизведётся на
живой волне 1, а не только на фикстурах — это НЕ «дефект G9», как помечено в
чек-листе B. Паттерн «отступающей гарантии»: второе противоречие B4 из round-1
спущено в фикстуры/контракт и переклеено в низкий приоритет.

Развязка: второй ингредиент эталона → `industrial_museum` везде (etalon_1.json,
api-contract пример, spec.md, stream-b US1/SC-B1, stream-d SC-D2). Продуктовое
решение оркестратора, исполняет архитектор на замороженном шве. Фейковые
industrial_site-POI в фикстуры не добавлять (против честности продукта).
Связь: [pairs-are-not-cut] (второе противоречие эталона), [coverage-dominates-ranking].

**Статус: исполнено 2026-08-19.** Шов выровнен; `cluster_id` эталона не менялся
(множество хабов то же). C не задет. A не трогали. В контракт добавлено
percent-encode `cluster_id` + reject comma в `hub.id` (уже в `make_hub_id`).

Поворкерно: Архитектор — 🔴 (владелец фикса: контракт+G9) + 🟡 url-encoding
cluster_id. A — 🔴 корень (ингест уже даёт industrial_museum; править эталон, не A).
B — 🔴 SC-B1 недостижим, метку «не блокер» поднять до блокера; дисциплина
точного матчинга верна. C — 🟢 не задет (cluster_id стабилен). D — 🟠 наследует,
приедет с фиксом контракта.

Стоп перед plan/tasks/implement: решение по ингредиенту эталона. После него
B/C/D зеленеют без переработки.

---

## 2026-08-19 (17:00) — Ревью этапа планирования: plan.md + data-model B/C/D

Тексты планов чистые: таблицы сверки инвариантов PASS у всех трёх, границы
верные, C фиксирует стабильность cluster_id, D не переранжирует. Команда
использует `knowledge/invariants/` как замену constitution Spec Kit — как и
задумано. Находки — в форме золотых фикстур, на которые планы опираются.

**🔴 Эталонная пара избыточна по покрытию.** Проверено `fixtures/rows/poi.json`:
Ярославль несёт И `ancient_temple`, И `industrial_museum` — то есть одиночный
Ярославль закрывает весь эталонный бургер (coverage=2). Ростов добавляет лишь
дублирующий храм. Одиночка и пара РАВНЫ по доминирующему ключу coverage →
исход решает cluster_score с НЕкалиброванными весами (F1). Следствия:
[coverage-dominates-ranking] здесь не защищает пару (US3 не применима: у
одиночки два matched); подпись демо «Ярославль И Ростов» держится на весах, не
на правиле; счётчик редкости и «переезд между городами» подорваны. Это латентное
противоречие B4 («второй ингредиент только в Ярославле» ⇒ Ярославль один
покрывает всё), ставшее проверяемым. Развязка: сделать города комплементарными
(убрать ancient_temple из фикстуры Ярославля, храм — у Ростова) ЛИБО признать
одиночный Ярославль честным ответом / переопределить бургер. Продуктовое решение
оркестратора, не подгонка весов. Владелец фикса — архитектор (G9).
Связь: [pairs-are-not-cut], [coverage-dominates-ranking].

**🟠 clusters.json маскирует проблему (Goodhart).** Предпосчёт содержит только
пару для эталона, не одиночного Ярославля с тем же покрытием. SC-B1 через кэш
позеленеет и скроет ничью; живой проход дисков её вскроет. Рекомендация: регресс
SC-B1/B2 гонять с пустой таблицей `cluster` (механический гейт под заявленный в
data-model принцип «live pass — источник правды»).

Поворкерно: Архитектор 🔴 (G9-фикстуры + предпосчёт). B 🟠 (наследует; план верно
говорит «live pass — правда», но SC-B1/US3 опираются на покрытийную отличность
пары, которую данные отрицают; усилить регресс пустым кэшем; поднять оркестратору,
не калибровать веса). C 🟢 не задет. D 🟢/🟠 минор (не переранжирует; наследует
через видимый критерий, приедет с фиксом G9).

Стоп перед tasks/implement: форма фикстур эталона (комплементарность городов) +
регресс на пустом кэше.

**Статус: исполнено оркестратором 2026-08-19 (defaults = 1b + 2a).** Продукт:
бургер не менять; храмы Ярославля не вырезать; одиночный Ярославль — законный
полный ответ; пара в топ-5, не обязательно #1; веса не подгонять. Механика:
SC-B1/B2 только при пустой `cluster`. C: тот же `cluster_id` пары из
`etalon_1.json`, не `places[0]`. D кликает пару для фазы 2.
